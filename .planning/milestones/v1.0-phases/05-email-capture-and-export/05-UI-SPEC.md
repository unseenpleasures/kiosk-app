---
phase: 5
slug: email-capture-and-export
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-21
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for Phase 5: Email Capture and Export.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.
>
> Phase 5 adds three UI surfaces to the existing app: (1) a persistent
> chrome email button visible on every screen, (2) the email sign-up
> screen with GDPR consent form and post-submit confirmation view, and
> (3) a CSV export section inside the existing admin panel. No new
> screens are introduced — `#/email` is the only new route. All design
> tokens are inherited from Phase 1 and remain unchanged.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — vanilla CSS custom properties |
| Preset | not applicable |
| Component library | none — browser-native elements only |
| Icon library | none — Unicode glyphs only (&#9993; envelope for email button, consistent with &#8962; home button) |
| Font | Bebas Neue (display) + Inter 400/700 (body/UI), self-hosted woff2 |

Source: CLAUDE.md (no-library policy); Phase 1 UI-SPEC (tokens established, no changes for Phase 5)

---

## Spacing Scale

Tokens are unchanged from Phase 1. Declared here for executor reference:

| Token | Value | Usage in Phase 5 |
|-------|-------|-----------------|
| xs | 4px | Checkbox-label gap, inline icon spacing |
| sm | 8px | Input internal padding (vertical), label-to-input gap |
| md | 16px | Default field spacing, consent group top margin |
| lg | 24px | Form card padding, section vertical rhythm |
| xl | 32px | Screen vertical centering offset |
| 2xl | 48px | Email screen top/bottom inset |
| 3xl | 64px | Not used in Phase 5 |

Exceptions:
- Touch targets: 48px minimum on all interactive elements — email input, checkbox tap area, submit button, early-dismiss button, chrome email button. This is the project-wide rule from Phase 1.
- Checkbox tap area: The `<label>` wrapping the checkbox must expand to a minimum 48px height to ensure a comfortable tap target on the kiosk. The visible checkbox glyph may be smaller; the tap area must not be.
- Email input: 48px minimum height (`min-height: var(--touch-target-min)`), same as all other inputs in the project.
- Submit button: 56px height when using `.btn-large` modifier — matches existing `btn-large` definition.

Source: Phase 1 UI-SPEC §Spacing Scale; main.css `:root` (verified); CONTEXT.md D-04, D-05, D-06

---

## Typography

Roles and values are unchanged from Phase 1. No new type roles are introduced in Phase 5.

| Role | Family | Size | Weight | Line Height | Usage in Phase 5 |
|------|--------|------|--------|-------------|-----------------|
| Display | Bebas Neue | 48px | 400 | 1.1 | Confirmation screen "Thank You" heading |
| Heading | Inter | 28px | 700 | 1.2 | Email screen title ("Sign Up for Updates") |
| Body | Inter | 16px | 400 | 1.5 | Form instructions, GDPR label text, confirmation body copy |
| Label | Inter | 14px | 700 | 1.2 | Submit button, "Back to browsing" button, admin export button, inline validation messages |

Notes:
- GDPR consent label uses Inter 16px weight 400 (body role) — not label role. The text is a sentence, not a UI label, and must be legible at reading distance on the kiosk.
- Countdown copy ("Returning to catalogue in 5…") uses Inter 16px weight 400 (body role).
- Error state copy uses Inter 14px weight 700 (`--color-destructive`) — label role with semantic colour.
- Admin export section heading uses `.admin-section-heading` — this is a pre-existing class defined in main.css (18px Inter 700) and is listed under Reused classes below. It is not a new Phase 5 type role.

Source: Phase 1 UI-SPEC §Typography; main.css `:root` (verified)

---

## Color

All tokens are unchanged from Phase 1. Phase 5 extends the accent reserved-for list.

| Role | Token | Hex | Usage |
|------|-------|-----|-------|
| Dominant (60%) | `--color-bg` | `#0d0f1a` | Email screen background, confirmation screen background |
| Secondary (30%) | `--color-surface` | `#0e1b4d` | Email form card background, admin export section background (via `.admin-section`) |
| Accent (10%) | `--color-accent` | `#f5c842` | See reserved-for list below |
| Text primary | `--color-text-primary` | `#f0f0f0` | All body and heading copy on email screen |
| Text secondary | `--color-text-secondary` | `#a0a8c0` | GDPR label text, countdown copy, hint text |
| Destructive | `--color-destructive` | `#e53e3e` | Inline validation error (invalid email format), dbAdd failure error message |

Accent reserved for (complete project list — Phase 5 additions marked NEW):
1. Primary CTA button background on splash screen
2. "Sync Required" heading text on the blocking screen
3. Interactive element pressed/active outline (2px ring)
4. Chrome home button border and icon colour
5. **(NEW)** Chrome email button border and icon colour — same visual pattern as chrome home button
6. **(NEW)** Submit button ("Sign me up") background — this is the primary CTA for the email screen, same role as the splash screen CTA
7. Admin panel heading text (existing, Phases 3–4)
8. Sync progress bar fill (existing, Phase 3)

Accent is NOT used for: confirmation screen icon, secondary/outline buttons, checkbox checked state (browser default), background fills other than primary CTAs.

Source: Phase 1 UI-SPEC §Color; CONTEXT.md D-06; RESEARCH.md §Pattern 1 (chrome email button CSS)

---

## Screen Specifications

### New Surface 1: Chrome Email Button

**Location:** `index.html` global chrome layer, fixed position.

**Position:** Bottom-left. `position: fixed; bottom: var(--space-md); left: var(--space-md)` (16px from both edges). This is the only available corner — top-left is occupied by `#chrome-home`, bottom-right and right side are occupied by `#chrome-qr`.

**Dimensions:** 48 × 48px, matching `#chrome-home` exactly.

**Visual:**
- Background: `rgba(14, 27, 77, 0.85)` — same translucent navy as `.chrome-home`
- Border: `1px solid var(--color-accent)` — same as `.chrome-home`
- Border radius: 8px — same as `.chrome-home`
- Icon: Unicode ✉ (`&#9993;`) at 22px — slightly smaller than the 24px home glyph to account for the envelope character's wider visual footprint
- Icon colour: `var(--color-accent)`
- z-index: 100 — same layer as `#chrome-home` and `#chrome-qr`

**Pressed state:** `opacity: 0.8` on `:active` — same pattern as `.chrome-home:active`

**Aria label:** `"Sign up for email updates"` — describes the destination, not the icon

**CSS class:** `.chrome-email`

Source: CONTEXT.md D-01, D-02, D-03; RESEARCH.md §Pattern 1; main.css `.chrome-home` (lines 284–305)

---

### New Surface 2: Email Sign-Up Screen (`#/email`)

**Triggered when:** User taps the chrome email button from any screen. Route: `#/email`. Renders via `renderEmail()` in `src/email.js`.

**Layout (landscape viewport 1366 × 1024 CSS px):**

The screen centres a single form card vertically and horizontally. The form card must not be full-viewport-width — it should feel like a focused panel, not a sprawling form.

- Screen: `display: flex; flex-direction: column; align-items: center; justify-content: center;` — inherits `.screen` base class
- Screen background: `--color-bg` (`#0d0f1a`)
- Form card: `background: var(--color-surface)`, `border-radius: 12px`, `padding: var(--space-xl)` (32px), `width: 100%; max-width: 560px`
- Screen must have `overflow-y: auto` to allow scroll if the keyboard pushes content — mitigates iOS keyboard overlay pitfall

**Form card contents, top to bottom:**

1. **Screen heading:** "Sign Up for Updates"
   - Inter 28px weight 700, `--color-text-primary`, `line-height: 1.2`
   - `margin-bottom: var(--space-md)` (16px)

2. **Sub-heading / instruction:** "Get early access to new designs and exclusive event discounts."
   - Inter 16px weight 400, `--color-text-secondary`, `line-height: 1.5`
   - `margin-bottom: var(--space-lg)` (24px)

3. **Email input:**
   - `<input type="email" inputmode="email" autocorrect="off" autocapitalize="off" spellcheck="false">`
   - Width: 100% of form card
   - Height: 48px minimum (`min-height: var(--touch-target-min)`)
   - Padding: `var(--space-sm) var(--space-md)` (8px vertical, 16px horizontal)
   - Background: `var(--color-bg)`, border: `1px solid var(--color-text-secondary)`, border-radius: 4px
   - Text: Inter 16px weight 400, `--color-text-primary`
   - Focus state: `border-color: var(--color-accent)`, no outline (same as `.admin-input:focus`)
   - Placeholder text: "your@email.com" — `--color-text-secondary`
   - `margin-bottom: var(--space-sm)` (8px)

4. **Inline validation error (conditional):**
   - Shown below the input when submit is tapped with an invalid email
   - Text: "Please enter a valid email address." — Inter 14px weight 700, `--color-destructive`
   - `margin-bottom: var(--space-sm)` (8px)
   - Hidden (`display: none`) until validation fails; shown inline, does not shift layout unexpectedly — reserve space with `min-height: 20px`

5. **GDPR consent group:**
   - Wrapping `<label>` element — the entire row (checkbox + text) is the tap target
   - Min-height: 48px, `display: flex; align-items: center; gap: var(--space-sm)` (8px)
   - Checkbox: `<input type="checkbox">` — browser default rendering, unchecked by default; do NOT set `checked` attribute
   - Checkbox visual size: 20 × 20px minimum; use `width: 20px; height: 20px; flex-shrink: 0` to prevent squishing
   - Label text: "I agree to receive email updates from The ID Card Factory." — Inter 16px weight 400, `--color-text-primary`, `line-height: 1.5`
   - The exact wording above is mandatory — GDPR compliance (CONTEXT.md D-05)
   - `margin-bottom: var(--space-lg)` (24px)

6. **Submit button:**
   - Text: "Sign me up"
   - Reuses `.btn-primary.btn-large` — full-width (100%), 56px height, `--color-accent` background, `--color-bg` text, Inter 16px weight 700
   - `disabled` attribute present on initial render; removed only when checkbox `change` event fires with `checked === true`
   - Disabled state: `opacity: 0.5; cursor: not-allowed` — existing `.btn-primary:disabled` rule applies
   - Pressed state (when enabled): `opacity: 0.8` on `:active`

**Idle timer:** The 3-minute grace period for `#/email` is already wired by `initEmailGracePeriod()` in `idle.js` — no additional idle handling required in `email.js`.

**Global chrome:** `#chrome-home`, `#chrome-qr`, and `#chrome-email` remain visible on this screen (they are in the global chrome layer above `#app`). The chrome email button should visually indicate the current screen — apply `opacity: 0.5` to `#chrome-email` when the current hash is `#/email` to signal it is already active. Wire this state in `app.js` on hashchange.

Source: CONTEXT.md D-04, D-05, D-06, D-07; RESEARCH.md §Pattern 2, §Pitfall 2, §Pitfall 3; REQUIREMENTS.md EMAIL-01, EMAIL-02

---

### New Surface 3: Email Confirmation View

**Triggered when:** `dbAdd('emails', record)` resolves successfully. Replaces the form content inside `#screen-email` in-place — no route change, no new DOM container (CONTEXT.md D-11).

**Layout:**

The confirmation view fills the same form card area as the sign-up form. The card content is replaced entirely.

- Form card background and dimensions unchanged (`--color-surface`, `max-width: 560px`)
- Contents, top to bottom:

1. **Success icon:**
   - Unicode checkmark: ✓ (`&#10003;`) — rendered at 48px, `--color-accent`, `text-align: center`
   - Do NOT use an SVG or external asset — Unicode glyph is sufficient and has zero cache overhead
   - `margin-bottom: var(--space-md)` (16px)

2. **Confirmation heading:** "You're in!"
   - Bebas Neue 48px, `--color-text-primary`, `line-height: 1.1`, `text-align: center`, `text-transform: uppercase`
   - `margin-bottom: var(--space-sm)` (8px)

3. **Confirmation body:** "Thanks for signing up. We'll let you know about new designs and event exclusives."
   - Inter 16px weight 400, `--color-text-secondary`, `line-height: 1.5`, `text-align: center`
   - `margin-bottom: var(--space-sm)` (8px)

4. **Signup count (optional, async):** "You're #N to sign up at this event!"
   - Inter 14px weight 400, `--color-text-secondary`, `line-height: 1.5`, `text-align: center`
   - Loaded asynchronously via `dbGetAll('emails')` filtered to current event; rendered once the promise resolves
   - Initially hidden; inserted into the DOM and made visible when the count resolves
   - If count fetch fails, this line is silently omitted — it is a nice-to-have, not critical
   - `margin-bottom: var(--space-lg)` (24px)

5. **Countdown text:** "Returning to catalogue in 5…"
   - Inter 16px weight 400, `--color-text-secondary`, `line-height: 1.5`, `text-align: center`
   - Updated in-place as the counter ticks: "…in 4…", "…in 3…", "…in 2…", "…in 1…"
   - Update via `textContent` on the countdown element — do NOT re-render the whole view
   - `margin-bottom: var(--space-md)` (16px)

6. **Early-dismiss button:** "Back to browsing"
   - Reuses `.btn-secondary.btn-large` — full-width, 56px height, transparent background, `--color-text-secondary` text and border
   - Tapping this clears the countdown interval and immediately navigates to `#/`

**Entry transition:**
- The form card content replacement is instantaneous — no fade or slide. The `.fade-in` class from `animations.css` is NOT applied here. Immediate render is correct: the user just submitted and should see the result without delay.

Source: CONTEXT.md D-08, D-09, D-10, D-11; RESEARCH.md §Pattern 2 §Open Question 2

---

### Modified Surface 4: Admin Panel — Email Export Section

**Location:** Inside `renderAdminPanel()` in `admin.js`. New `admin-section` div inserted before the Exit button, after the existing Sync Status section.

**Visual:** Matches all existing `admin-section` cards exactly — same `--color-surface` background, `border-radius: 8px`, `padding: var(--space-lg)`.

**Section heading:** "Email Export"
- `.admin-section-heading` — pre-existing class in main.css (Inter 18px weight 700, `--color-text-primary`); see Reused classes below

**Export button:**
- Text: "Export / Share Emails (CSV)"
- The label explicitly says "Share" because iPadOS Safari presents a share sheet rather than a silent download. This sets correct admin expectations (RESEARCH.md §Pitfall 4).
- Reuses `.btn-primary.btn-large` — full-width, 56px height
- `margin-top: var(--space-md)` (16px below heading)

**Status message (conditional):**
- Zero emails state: "No emails captured for this event yet." — `.sync-result-detail` class — Inter 14px weight 400, `--color-text-secondary`. Shown inline below button after tap; button remains tappable.
- Export success state: "Exported N email(s)." — `.sync-result-detail` class — same style. Shown inline below button after successful download.
- Both states use the existing `.sync-result-detail` CSS class — no new CSS needed.
- Status message element is rendered at mount (`display: none`), shown on tap result — avoids layout shift.

Source: CONTEXT.md D-14, D-15, D-16, D-17, D-18, D-19; RESEARCH.md §Pattern 3; main.css `.admin-section`, `.admin-section-heading`, `.btn-primary`, `.btn-large`, `.sync-result-detail`

---

### Error State: dbAdd Failure

**Triggered when:** `dbAdd('emails', record)` rejects. This is rare (device storage full, IDB corruption) but must not silently discard the user's submission.

**Location:** Shown inline within the email form — below the submit button, replacing the disabled state of the button temporarily.

**Visual:**
- Error message: "Something went wrong saving your details. Please try again." — Inter 14px weight 700, `--color-destructive`, `text-align: center`
- Submit button: re-enabled immediately after the error, allowing the user to retry
- Form fields retain their values (email address and checkbox state are not cleared)
- The `record` object (email, eventName, eventDate, consentAt) must be logged to `console.error` for admin diagnostics — data must not be silently dropped

Source: CONTEXT.md (Claude's Discretion — error state); RESEARCH.md §Pattern 2 `showEmailError()`; REQUIREMENTS.md EMAIL-03

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Chrome email button aria-label | "Sign up for email updates" |
| Email screen heading | "Sign Up for Updates" |
| Email screen sub-heading | "Get early access to new designs and exclusive event discounts." |
| Email input placeholder | "your@email.com" |
| GDPR consent label | "I agree to receive email updates from The ID Card Factory." |
| Submit button (primary CTA) | "Sign me up" |
| Inline validation error | "Please enter a valid email address." |
| Confirmation heading | "You're in!" |
| Confirmation body | "Thanks for signing up. We'll let you know about new designs and event exclusives." |
| Signup count | "You're #N to sign up at this event!" |
| Countdown text (template) | "Returning to catalogue in N…" |
| Early-dismiss button | "Back to browsing" |
| dbAdd failure error | "Something went wrong saving your details. Please try again." |
| Admin export section heading | "Email Export" |
| Admin export button | "Export / Share Emails (CSV)" |
| Admin export — zero emails | "No emails captured for this event yet." |
| Admin export — success | "Exported N email(s)." |

Tone: Direct and warm for the customer-facing form. Functional and precise for the admin export section. No exclamation marks on operational messages. No marketing language on error states.

GDPR note: The consent label wording is legally significant — it must not be paraphrased, abbreviated, or reordered. Use verbatim: "I agree to receive email updates from The ID Card Factory."

Source: CONTEXT.md D-05 (GDPR wording fixed), D-08, D-09, D-10; Phase 1 UI-SPEC §Copywriting Contract (tone reference)

---

## Interaction States Summary

| Element | Default | Active/Pressed | Disabled | Error |
|---------|---------|---------------|----------|-------|
| Chrome email button | Visible, `--color-accent` border/icon | `opacity: 0.8` | n/a | n/a |
| Chrome email button (on `#/email`) | `opacity: 0.5` (already on this screen) | `opacity: 0.8` | n/a | n/a |
| Email input | `border: 1px solid --color-text-secondary` | Focus: `border-color: --color-accent` | n/a | n/a (no error border — validation error shown below) |
| GDPR checkbox | Unchecked (browser default) | Checked (browser default) | n/a | n/a |
| Submit button | `opacity: 0.5`, `cursor: not-allowed` (disabled) | `opacity: 0.8` on `:active` when enabled | Disabled by default; enabled when checkbox checked | Re-enabled after dbAdd failure |
| Early-dismiss button | `.btn-secondary` default | `opacity: 0.8` on `:active` | n/a | n/a |
| Admin export button | `.btn-primary` default | `opacity: 0.8` on `:active` | n/a | n/a |

---

## New CSS Classes Required

The following new CSS classes must be added to `styles/main.css`. All reuse existing token variables — no new token values.

| Class | Description |
|-------|-------------|
| `.chrome-email` | Fixed bottom-left chrome button — mirrors `.chrome-home` at bottom:var(--space-md) left:var(--space-md) |
| `.chrome-email.active-screen` | Reduced opacity (`0.5`) when current route is `#/email` |
| `.screen-email` | Email screen container — inherits `.screen` base; adds `overflow-y: auto` for keyboard scroll safety |
| `.email-form-card` | Form card — `background: var(--color-surface); border-radius: 12px; padding: var(--space-xl); width: 100%; max-width: 560px` |
| `.email-screen-heading` | Screen heading — Inter 28px weight 700, `--color-text-primary` |
| `.email-screen-subheading` | Sub-heading — Inter 16px weight 400, `--color-text-secondary` |
| `.email-input` | Email field — inherits `.admin-input` visual rules; add `inputmode` and autocorrect attributes in HTML |
| `.email-validation-error` | Inline validation error — Inter 14px weight 700, `--color-destructive`; `min-height: 20px` |
| `.email-consent-group` | Label wrapping checkbox + text — `display: flex; align-items: center; gap: var(--space-sm); min-height: 48px` |
| `.email-consent-checkbox` | Checkbox sizing — `width: 20px; height: 20px; flex-shrink: 0` |
| `.email-submit` | Alias for `.btn-primary.btn-large` applied to submit — no new styles, just semantic naming in JS |
| `.email-confirmation` | Confirmation card state — same `.email-form-card` container, no extra CSS needed; content replaced in JS |
| `.email-confirmation-icon` | Unicode checkmark — `font-size: 48px; color: var(--color-accent); text-align: center; display: block` |
| `.email-confirmation-heading` | Bebas Neue 48px — reuses `.splash-title` visual rules (same font/size/transform); can reuse that class or duplicate |
| `.email-confirmation-body` | Confirmation body copy — Inter 16px weight 400, `--color-text-secondary` |
| `.email-confirmation-count` | Async signup count — Inter 14px weight 400, `--color-text-secondary`; `display: none` until resolved |
| `.email-countdown` | Countdown line — Inter 16px weight 400, `--color-text-secondary` |

Reused classes (no new CSS needed):
- `.btn-primary`, `.btn-secondary`, `.btn-large` — submit and early-dismiss buttons
- `.admin-section`, `.admin-section-heading` — email export section in admin panel; `.admin-section-heading` is a pre-existing class (Inter 18px weight 700) defined in main.css and is not a Phase 5 type role
- `.sync-result-detail` — export status messages
- `.screen` — base screen class

Source: RESEARCH.md §Pattern 1 (chrome button CSS), §Pattern 2 (form structure); main.css (verified reusable classes)

---

## Animation Contract

No new animation classes for Phase 5. Rules are inherited from the project-wide animation contract:

| Transition | Rule |
|------------|------|
| Chrome email button press | `opacity: 0.8` on `:active` — CSS only, no JS |
| Form → confirmation swap | Instantaneous DOM replacement — no transition; user submitted and must see result immediately |
| Countdown text update | `textContent` swap — no transition |
| Submit button enable/disable | Attribute toggle — no transition |
| `.fade-in` | NOT applied to email screen or confirmation — reserved for splash screen only |

A9X constraint: No `transform`, no `@keyframes`, no `will-change` on any new elements.

Source: Phase 1 UI-SPEC §Interaction Constraints; animations.css (confirmed: only `.fade-in` opacity transition)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — not applicable | not applicable (vanilla CSS project) |
| Third-party | none | not applicable |

No component registry in use. All UI is hand-authored vanilla HTML + CSS per CLAUDE.md no-library policy.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: 05-email-capture-and-export*
*UI-SPEC created: 2026-03-21*
*Status: draft — awaiting gsd-ui-checker verification*
