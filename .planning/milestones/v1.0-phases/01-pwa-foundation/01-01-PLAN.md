---
phase: 01-pwa-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - manifest.json
  - styles/main.css
  - styles/animations.css
  - assets/logo.svg
  - assets/qr-code.png
  - assets/icons/icon-512.png
  - assets/icons/icon-1024.png
  - assets/splash-2732x2048.png
  - assets/splash-2048x2732.png
  - assets/fonts/bebas-neue.woff2
  - assets/fonts/inter-400.woff2
  - assets/fonts/inter-700.woff2
autonomous: true
requirements:
  - PWA-01
  - PWA-05

must_haves:
  truths:
    - "App icon displays correctly at 512x512 and 1024x1024 on iPad home screen"
    - "Tapping home screen icon opens app in standalone mode with dark background and no Safari chrome"
    - "All brand colors, fonts, and spacing tokens are defined as CSS custom properties in one place"
    - "No external network requests are made for fonts, icons, or CSS"
  artifacts:
    - path: "index.html"
      provides: "App shell with manifest link, apple-touch-icon tags, apple-touch-startup-image tags, meta tags for standalone mode"
      contains: "apple-touch-icon"
    - path: "manifest.json"
      provides: "PWA manifest with standalone display, landscape orientation, dark theme, icon references"
      contains: "standalone"
    - path: "styles/main.css"
      provides: "CSS custom properties for colors, spacing, typography; @font-face declarations; landscape orientation fix; base reset"
      contains: "--color-bg"
    - path: "styles/animations.css"
      provides: "Splash screen fade-in CSS transition"
      contains: "opacity"
    - path: "assets/icons/icon-512.png"
      provides: "512x512 PWA icon with dark background and logo"
    - path: "assets/icons/icon-1024.png"
      provides: "1024x1024 PWA icon with dark background and logo"
  key_links:
    - from: "index.html"
      to: "manifest.json"
      via: "link rel=manifest"
      pattern: "rel=\"manifest\""
    - from: "index.html"
      to: "assets/icons/icon-1024.png"
      via: "apple-touch-icon link tag"
      pattern: "apple-touch-icon"
    - from: "index.html"
      to: "styles/main.css"
      via: "stylesheet link"
      pattern: "rel=\"stylesheet\""
    - from: "styles/main.css"
      to: "assets/fonts/bebas-neue.woff2"
      via: "@font-face src url"
      pattern: "bebas-neue\\.woff2"
---

<objective>
Create the complete project scaffolding for Phase 1: manifest.json, index.html app shell with all iOS-specific meta tags, CSS foundation with brand tokens and font-face declarations, and all static assets (icons, fonts, logo, QR code, splash images).

Purpose: This is the installable PWA skeleton. Without correct manifest fields, apple-touch-icon tags, and splash images, the app cannot install properly on iPad or display the correct icon and launch screen.

Output: A valid PWA that can be added to an iPad home screen with the correct icon, standalone display, dark theme, and branded launch splash — even before any JS logic exists.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pwa-foundation/01-CONTEXT.md
@.planning/phases/01-pwa-foundation/01-RESEARCH.md
@.planning/phases/01-pwa-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download brand assets and create icon/splash image files</name>
  <files>
    assets/logo.svg,
    assets/qr-code.png,
    assets/icons/icon-512.png,
    assets/icons/icon-1024.png,
    assets/splash-2732x2048.png,
    assets/splash-2048x2732.png,
    assets/fonts/bebas-neue.woff2,
    assets/fonts/inter-400.woff2,
    assets/fonts/inter-700.woff2
  </files>
  <read_first>
    .planning/phases/01-pwa-foundation/01-RESEARCH.md,
    .planning/phases/01-pwa-foundation/01-UI-SPEC.md
  </read_first>
  <action>
    Create the directory structure: `assets/icons/`, `assets/fonts/`.

    **Logo (per D-01):**
    Download the ID Card Factory logo from `https://theidcardfactory.co.uk/cdn/shop/files/id-card-factory-logo.svg` and save as `assets/logo.svg`. If the URL fails, create a text-based SVG placeholder with "ID Card Factory" in gold (#f5c842) on transparent background, sized to fit within 320px width.

    **App icons (per D-02, D-03, D-04):**
    Generate `assets/icons/icon-512.png` (512x512px) and `assets/icons/icon-1024.png` (1024x1024px). Each icon:
    - Background fill: `#0d0f1a` (dark navy)
    - ID Card Factory logo centered (use the downloaded logo.svg)
    - Square format, no rounded corners (iOS applies mask automatically)

    Use Python with Pillow, or use an SVG-to-PNG conversion tool, or create the icons programmatically. If the logo SVG download fails, create a simple text-based icon with "IDCF" in Bebas Neue or a bold sans-serif, gold (#f5c842) on dark navy (#0d0f1a) background.

    **Splash images (per RESEARCH.md Pattern 2, UI-SPEC Asset Requirements):**
    Generate `assets/splash-2732x2048.png` (landscape) and `assets/splash-2048x2732.png` (portrait). Both:
    - Background fill: `#0d0f1a`
    - Logo centered (same logo as icons)
    - These are `apple-touch-startup-image` assets shown during PWA launch

    **QR code (per D-09, UI-SPEC):**
    Generate `assets/qr-code.png` — a QR code pointing to `https://theidcardfactory.co.uk`. Size: 192x192px source (rendered at 96x96 CSS px for Retina). Use Python `qrcode` library (`pip install qrcode[pil]`) or any CLI QR generator. White QR modules on transparent or dark background.

    **Fonts (per RESEARCH.md §Fonts):**
    Download and save:
    - `assets/fonts/bebas-neue.woff2` from https://github.com/dharmatype/Bebas-Neue or https://gwfh.mranftl.com/fonts/bebas-neue?subsets=latin (select woff2 format)
    - `assets/fonts/inter-400.woff2` from https://gwfh.mranftl.com/fonts/inter?subsets=latin (weight 400, woff2)
    - `assets/fonts/inter-700.woff2` from https://gwfh.mranftl.com/fonts/inter?subsets=latin (weight 700, woff2)

    If font download fails from these URLs, use alternative sources: Google Fonts API direct download, or the fonts bundled in system packages. The files MUST be woff2 format.
  </action>
  <verify>
    <automated>ls -la assets/logo.svg assets/qr-code.png assets/icons/icon-512.png assets/icons/icon-1024.png assets/splash-2732x2048.png assets/splash-2048x2732.png assets/fonts/bebas-neue.woff2 assets/fonts/inter-400.woff2 assets/fonts/inter-700.woff2</automated>
  </verify>
  <acceptance_criteria>
    - assets/logo.svg exists and file size > 0
    - assets/qr-code.png exists and file size > 0
    - assets/icons/icon-512.png exists and file size > 0
    - assets/icons/icon-1024.png exists and file size > 0
    - assets/splash-2732x2048.png exists and file size > 0
    - assets/splash-2048x2732.png exists and file size > 0
    - assets/fonts/bebas-neue.woff2 exists and file size > 0
    - assets/fonts/inter-400.woff2 exists and file size > 0
    - assets/fonts/inter-700.woff2 exists and file size > 0
  </acceptance_criteria>
  <done>All 9 asset files exist at the correct paths with non-zero file sizes. Icons are 512x512 and 1024x1024 PNG with dark navy background. Splash images are correct dimensions. QR code encodes https://theidcardfactory.co.uk. Three woff2 font files present.</done>
</task>

<task type="auto">
  <name>Task 2: Create manifest.json and index.html app shell with iOS meta tags</name>
  <files>
    manifest.json,
    index.html
  </files>
  <read_first>
    .planning/phases/01-pwa-foundation/01-RESEARCH.md,
    .planning/phases/01-pwa-foundation/01-UI-SPEC.md,
    .planning/phases/01-pwa-foundation/01-CONTEXT.md
  </read_first>
  <action>
    **manifest.json (per RESEARCH.md Pattern 1, PWA-01):**
    Create `manifest.json` in project root with exactly these fields:
    ```json
    {
      "name": "ID Card Factory",
      "short_name": "ID Cards",
      "start_url": "/",
      "scope": "/",
      "display": "standalone",
      "background_color": "#0d0f1a",
      "theme_color": "#0d0f1a",
      "orientation": "landscape",
      "icons": [
        {
          "src": "/assets/icons/icon-512.png",
          "sizes": "512x512",
          "type": "image/png"
        },
        {
          "src": "/assets/icons/icon-1024.png",
          "sizes": "1024x1024",
          "type": "image/png"
        }
      ]
    }
    ```

    **index.html (per RESEARCH.md Pattern 2, UI-SPEC Screen Specs):**
    Create `index.html` in project root. The file must contain:

    `<head>` section:
    - `<meta charset="UTF-8">`
    - `<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">`
    - `<meta name="theme-color" content="#0d0f1a">`
    - `<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">`
    - `<meta name="apple-mobile-web-app-title" content="ID Cards">`
    - `<link rel="manifest" href="/manifest.json">`
    - `<link rel="apple-touch-icon" sizes="1024x1024" href="/assets/icons/icon-1024.png">`
    - `<link rel="apple-touch-icon" sizes="512x512" href="/assets/icons/icon-512.png">`
    - `<link rel="apple-touch-startup-image" media="(device-width: 1024px) and (device-height: 1366px) and (-webkit-device-pixel-ratio: 2) and (orientation: landscape)" href="/assets/splash-2732x2048.png">`
    - `<link rel="apple-touch-startup-image" media="(device-width: 1024px) and (device-height: 1366px) and (-webkit-device-pixel-ratio: 2) and (orientation: portrait)" href="/assets/splash-2048x2732.png">`
    - `<link rel="stylesheet" href="/styles/main.css">`
    - `<link rel="stylesheet" href="/styles/animations.css">`
    - `<title>ID Card Factory</title>`

    Do NOT include `<meta name="apple-mobile-web-app-capable">` — it is deprecated per RESEARCH.md anti-patterns.

    `<body>` section:
    - `<body style="background-color: #0d0f1a;">` (inline bg prevents white flash before CSS loads)
    - `<div id="app"></div>` — root container for all view rendering
    - `<script src="/src/app.js" defer></script>` — main app script (created in Plan 02)

    The body should contain no other visible content — all screens are rendered by app.js into `#app`.
  </action>
  <verify>
    <automated>grep -c "apple-touch-icon" index.html && grep -c "manifest" index.html && grep -c "apple-touch-startup-image" index.html && grep -c "apple-mobile-web-app-capable" index.html || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `rel="manifest"` link to `/manifest.json`
    - index.html contains two `apple-touch-icon` link tags (512 and 1024)
    - index.html contains two `apple-touch-startup-image` link tags with correct media queries for iPad Pro 12.9" (device-width: 1024px, device-height: 1366px, DPR 2)
    - index.html contains `meta name="apple-mobile-web-app-status-bar-style" content="black-translucent"`
    - index.html contains `meta name="apple-mobile-web-app-title" content="ID Cards"`
    - index.html contains `<div id="app"></div>`
    - index.html contains `<script src="/src/app.js" defer>`
    - index.html does NOT contain `apple-mobile-web-app-capable` (deprecated)
    - index.html contains `rel="stylesheet" href="/styles/main.css"`
    - index.html contains `rel="stylesheet" href="/styles/animations.css"`
    - manifest.json contains `"display": "standalone"`
    - manifest.json contains `"background_color": "#0d0f1a"`
    - manifest.json contains `"theme_color": "#0d0f1a"`
    - manifest.json contains `"orientation": "landscape"`
    - manifest.json contains icon entries for 512x512 and 1024x1024
  </acceptance_criteria>
  <done>manifest.json exists with standalone display, dark theme colors, landscape orientation, and both icon sizes. index.html exists with all required iOS meta tags, manifest link, stylesheet links, startup image tags, #app container, and deferred app.js script. No deprecated apple-mobile-web-app-capable tag present.</done>
</task>

<task type="auto">
  <name>Task 3: Create CSS foundation — tokens, font-face, reset, landscape fix, animations</name>
  <files>
    styles/main.css,
    styles/animations.css
  </files>
  <read_first>
    .planning/phases/01-pwa-foundation/01-UI-SPEC.md,
    .planning/phases/01-pwa-foundation/01-RESEARCH.md
  </read_first>
  <action>
    **styles/main.css (per D-06, D-07, UI-SPEC §CSS Custom Property Declarations):**

    Create `styles/` directory and `styles/main.css` with the following sections in order:

    1. **@font-face declarations** (per RESEARCH.md Pattern 7):
    ```css
    @font-face {
      font-family: 'Bebas Neue';
      font-style: normal;
      font-weight: 400;
      font-display: swap;
      src: url('/assets/fonts/bebas-neue.woff2') format('woff2');
    }

    @font-face {
      font-family: 'Inter';
      font-style: normal;
      font-weight: 400;
      font-display: swap;
      src: url('/assets/fonts/inter-400.woff2') format('woff2');
    }

    @font-face {
      font-family: 'Inter';
      font-style: normal;
      font-weight: 700;
      font-display: swap;
      src: url('/assets/fonts/inter-700.woff2') format('woff2');
    }
    ```

    2. **:root custom properties** — copy EXACTLY from UI-SPEC §CSS Custom Property Declarations:
    ```css
    :root {
      --color-bg:             #0d0f1a;
      --color-surface:        #0e1b4d;
      --color-accent:         #f5c842;
      --color-text-primary:   #f0f0f0;
      --color-text-secondary: #a0a8c0;
      --color-destructive:    #e53e3e;

      --space-xs:  4px;
      --space-sm:  8px;
      --space-md:  16px;
      --space-lg:  24px;
      --space-xl:  32px;
      --space-2xl: 48px;
      --space-3xl: 64px;

      --font-display: 'Bebas Neue', sans-serif;
      --font-body:    'Inter', sans-serif;

      --text-display: 48px;
      --text-heading: 28px;
      --text-body:    16px;
      --text-label:   14px;

      --weight-regular: 400;
      --weight-bold:    700;

      --leading-tight:  1.1;
      --leading-snug:   1.2;
      --leading-normal: 1.5;

      --touch-target-min: 48px;
    }
    ```

    3. **Base reset:**
    ```css
    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html, body {
      height: 100%;
      width: 100%;
      overflow: hidden;
      font-family: var(--font-body);
      font-size: var(--text-body);
      font-weight: var(--weight-regular);
      line-height: var(--leading-normal);
      color: var(--color-text-primary);
      background-color: var(--color-bg);
      -webkit-font-smoothing: antialiased;
      -webkit-tap-highlight-color: transparent;
      -webkit-text-size-adjust: 100%;
      user-select: none;
      -webkit-user-select: none;
    }

    #app {
      height: 100%;
      width: 100%;
      position: relative;
    }
    ```

    4. **Landscape orientation enforcement** (per RESEARCH.md Pattern 3):
    ```css
    @media (orientation: portrait) {
      body {
        transform: rotate(90deg);
        transform-origin: bottom left;
        width: 100vh;
        height: 100vw;
        overflow-x: hidden;
        position: absolute;
        top: -100vw;
        left: 0;
      }
    }
    ```

    5. **Splash screen styles** (per UI-SPEC Screen 1):
    ```css
    .screen {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100%;
      width: 100%;
      position: absolute;
      top: 0;
      left: 0;
    }

    .splash-screen {
      cursor: pointer;
    }

    .splash-logo {
      max-width: 320px;
      height: auto;
    }

    .splash-title {
      font-family: var(--font-display);
      font-size: var(--text-display);
      color: var(--color-text-primary);
      line-height: var(--leading-tight);
      margin-top: var(--space-lg);
      text-transform: uppercase;
      letter-spacing: 2px;
    }

    .splash-cta {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background-color: var(--color-accent);
      color: var(--color-bg);
      font-family: var(--font-body);
      font-size: var(--text-label);
      font-weight: var(--weight-bold);
      line-height: var(--leading-snug);
      padding: 0 var(--space-lg);
      min-height: var(--touch-target-min);
      border: none;
      border-radius: 4px;
      margin-top: var(--space-xl);
      cursor: pointer;
      text-decoration: none;
    }

    .splash-cta:active {
      opacity: 0.8;
    }
    ```

    6. **QR code corner placement** (per UI-SPEC Screen 1):
    ```css
    .qr-corner {
      position: absolute;
      bottom: var(--space-lg);
      right: var(--space-lg);
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: var(--space-xs);
    }

    .qr-corner img {
      width: 96px;
      height: 96px;
    }

    .qr-corner-label {
      font-family: var(--font-body);
      font-size: 12px;
      color: var(--color-text-secondary);
    }
    ```

    7. **Sync required screen styles** (per UI-SPEC Screen 2):
    ```css
    .sync-required-logo {
      max-width: 240px;
      height: auto;
    }

    .sync-required-heading {
      font-family: var(--font-body);
      font-size: var(--text-heading);
      font-weight: var(--weight-bold);
      color: var(--color-accent);
      line-height: var(--leading-snug);
      margin-top: var(--space-lg);
    }

    .sync-required-body {
      font-family: var(--font-body);
      font-size: var(--text-body);
      font-weight: var(--weight-regular);
      color: var(--color-text-primary);
      line-height: var(--leading-normal);
      margin-top: var(--space-md);
      max-width: 480px;
      text-align: center;
    }
    ```

    **styles/animations.css (per UI-SPEC Screen 1 Entry Animation):**

    Create `styles/animations.css` with:
    ```css
    .fade-in {
      opacity: 0;
      transition: opacity 300ms ease-in;
    }

    .fade-in.visible {
      opacity: 1;
    }
    ```

    No other animations. No `@keyframes`. No `transform3d`. Opacity transition only — safest for A9X GPU per UI-SPEC interaction constraints.
  </action>
  <verify>
    <automated>grep -c "\-\-color-bg" styles/main.css && grep -c "@font-face" styles/main.css && grep -c "orientation: portrait" styles/main.css && grep -c "fade-in" styles/animations.css</automated>
  </verify>
  <acceptance_criteria>
    - styles/main.css contains `:root` block with all 6 color tokens (--color-bg, --color-surface, --color-accent, --color-text-primary, --color-text-secondary, --color-destructive)
    - styles/main.css contains 7 spacing tokens (--space-xs through --space-3xl)
    - styles/main.css contains --font-display and --font-body tokens
    - styles/main.css contains --text-display (48px), --text-heading (28px), --text-body (16px), --text-label (14px)
    - styles/main.css contains --touch-target-min: 48px
    - styles/main.css contains 3 @font-face declarations (Bebas Neue, Inter 400, Inter 700) with `font-display: swap`
    - styles/main.css contains `@media (orientation: portrait)` with `transform: rotate(90deg)`
    - styles/main.css contains `.splash-cta` with `background-color: var(--color-accent)` and `min-height: var(--touch-target-min)`
    - styles/main.css contains `.sync-required-heading` with `color: var(--color-accent)`
    - styles/animations.css contains `.fade-in` class with `opacity` and `transition`
    - styles/animations.css contains `.fade-in.visible` with `opacity: 1`
    - styles/animations.css does NOT contain `@keyframes` or `transform3d`
  </acceptance_criteria>
  <done>styles/main.css contains all CSS custom properties from UI-SPEC, 3 font-face declarations with swap, base reset, portrait-to-landscape rotation fix, splash screen styles, sync-required screen styles, and QR corner styles. styles/animations.css contains the fade-in opacity transition class. No JS animations, no @keyframes, no prohibited CSS features.</done>
</task>

</tasks>

<verification>
1. All 9 asset files exist with non-zero file sizes
2. manifest.json is valid JSON with standalone display, dark theme, landscape orientation, both icon sizes
3. index.html has manifest link, apple-touch-icon tags, startup-image tags, stylesheet links, #app div, deferred app.js script
4. index.html does NOT have deprecated apple-mobile-web-app-capable tag
5. styles/main.css has all tokens from UI-SPEC, 3 font-face declarations, landscape fix
6. styles/animations.css has fade-in transition, no @keyframes
7. Serving index.html via local HTTP server shows a dark page with no console errors (CSS loads, no broken asset references)
</verification>

<success_criteria>
- PWA manifest is valid and references both icon sizes (PWA-01 partial)
- index.html has all iOS-specific meta tags for proper iPad install behavior (PWA-01 partial)
- All CSS tokens match UI-SPEC exactly — no missing or mismatched values
- All font files are local woff2 — no CDN references anywhere
- Portrait orientation media query rotates to landscape (PWA-01 orientation)
- Splash and sync-required screen styles are ready for app.js to render into
</success_criteria>

<output>
After completion, create `.planning/phases/01-pwa-foundation/01-01-SUMMARY.md`
</output>
