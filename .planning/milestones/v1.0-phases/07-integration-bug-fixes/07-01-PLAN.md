---
phase: 07-integration-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sw.js
  - styles/main.css
autonomous: true
requirements:
  - PWA-03
  - SYNC-04
gap_closure: true

must_haves:
  truths:
    - "Product images in kiosk-v3 cache survive a service worker version bump"
    - "Email count stat paragraph in admin analytics renders with accent colour and bold weight"
  artifacts:
    - path: "sw.js"
      provides: "Activate handler that preserves sync image cache"
      contains: "SYNC_CACHE_NAME"
    - path: "styles/main.css"
      provides: "CSS rule for .admin-stat class"
      contains: ".admin-stat"
  key_links:
    - from: "sw.js"
      to: "src/sync.js"
      via: "SYNC_CACHE_NAME constant value must match"
      pattern: "kiosk-v3"
---

<objective>
Fix service worker cache cleanup destroying product images on version bump, and add missing .admin-stat CSS rule.

Purpose: The SW activate handler currently deletes every cache except the app shell cache. This means bumping CACHE_NAME (e.g. kiosk-v8 to kiosk-v9) silently destroys all product images in the kiosk-v3 sync cache. The CSS fix addresses an unstyled email count stat in admin analytics.

Output: Updated sw.js with dual-cache exclusion, updated main.css with .admin-stat rule.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-integration-bug-fixes/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SW activate handler to preserve sync image cache</name>
  <files>sw.js</files>
  <read_first>
    - sw.js (current activate handler at lines 44-56, CACHE_NAME at line 5)
    - src/sync.js (SYNC_CACHE_NAME = 'kiosk-v3' at line 14)
  </read_first>
  <action>
Edit the activate event handler in sw.js (lines 44-56). Add a local constant `SYNC_CACHE_NAME = 'kiosk-v3'` with a comment `// MUST match SYNC_CACHE_NAME in src/sync.js` immediately before the activate listener. Then change the filter condition from:

```javascript
.filter(function(key) { return key !== CACHE_NAME; })
```

to:

```javascript
.filter(function(key) { return key !== CACHE_NAME && key !== SYNC_CACHE_NAME; })
```

Keep everything else in the activate handler identical: `event.waitUntil(...)`, `caches.keys().then(...)`, `Promise.all(...)`, `self.clients.claim()`.

Do NOT rename SYNC_CACHE_NAME. Do NOT merge image cache into app shell cache. Do NOT use Array.indexOf pattern -- the inline two-condition check is sufficient for two caches.

Also add a matching comment in src/sync.js next to the existing `var SYNC_CACHE_NAME = 'kiosk-v3';` line (line 14):
```javascript
var SYNC_CACHE_NAME = 'kiosk-v3'; // MUST match the SYNC_CACHE_NAME constant in sw.js activate handler
```

This is a comment-only change to sync.js -- do not change any logic.
  </action>
  <verify>
    <automated>grep -n "SYNC_CACHE_NAME" sw.js && grep -n "key !== CACHE_NAME && key !== SYNC_CACHE_NAME" sw.js && grep -n "MUST match" src/sync.js</automated>
  </verify>
  <acceptance_criteria>
    - sw.js contains the line: var SYNC_CACHE_NAME = 'kiosk-v3';
    - sw.js contains the line: // MUST match SYNC_CACHE_NAME in src/sync.js
    - sw.js activate filter contains: key !== CACHE_NAME && key !== SYNC_CACHE_NAME
    - sw.js still contains: self.clients.claim();
    - src/sync.js line 14 contains comment: // MUST match the SYNC_CACHE_NAME constant in sw.js activate handler
    - No other logic changes in either file
  </acceptance_criteria>
  <done>SW activate handler preserves both kiosk-v8 (app shell) and kiosk-v3 (sync images) caches; cross-file coupling documented with comments</done>
</task>

<task type="auto">
  <name>Task 2: Add missing .admin-stat CSS rule</name>
  <files>styles/main.css</files>
  <read_first>
    - styles/main.css (lines 595-620, specifically after .sync-status-warning at line 613)
    - src/admin.js (line 541 where emailStat.className = 'admin-stat' is set)
  </read_first>
  <action>
Add a new CSS rule for `.admin-stat` in styles/main.css immediately after the `.sync-status-warning` rule block (which ends at line 614). Insert the following exact CSS:

```css

/* admin.js analytics summary — email count stat paragraph */
.admin-stat {
  color: var(--color-accent);
  font-weight: var(--weight-bold);
  font-size: var(--text-body);
  margin-top: var(--space-sm);
}
```

This uses existing CSS custom properties already defined in the file: --color-accent (gold/amber), --weight-bold (700), --text-body, --space-sm. Do NOT introduce any new custom properties. Do NOT change any existing rules.
  </action>
  <verify>
    <automated>grep -A5 "\.admin-stat" styles/main.css</automated>
  </verify>
  <acceptance_criteria>
    - styles/main.css contains the rule: .admin-stat {
    - The rule contains: color: var(--color-accent);
    - The rule contains: font-weight: var(--weight-bold);
    - The rule contains: font-size: var(--text-body);
    - The rule contains: margin-top: var(--space-sm);
    - The rule appears after .sync-status-warning
    - No existing rules are modified
  </acceptance_criteria>
  <done>Email count stat in admin analytics renders with accent colour, bold weight, body font size, and top margin</done>
</task>

</tasks>

<verification>
1. `grep "SYNC_CACHE_NAME" sw.js` shows the constant and the filter condition
2. `grep "key !== CACHE_NAME && key !== SYNC_CACHE_NAME" sw.js` confirms dual-cache exclusion
3. `grep -A5 ".admin-stat" styles/main.css` shows the complete rule with all four properties
4. No other files modified besides sw.js, src/sync.js (comment only), and styles/main.css
</verification>

<success_criteria>
- The sw.js activate handler preserves both CACHE_NAME and SYNC_CACHE_NAME caches during cleanup
- The .admin-stat CSS rule exists in main.css with accent colour, bold weight, body font size, and spacing
- Cross-file SYNC_CACHE_NAME coupling is documented with comments in both sw.js and sync.js
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-bug-fixes/07-01-SUMMARY.md`
</output>
