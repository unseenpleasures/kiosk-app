---
phase: 08-deploy-and-install
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - .planning/INSTALL-GUIDE.md
autonomous: false
requirements:
  - HOST-01
  - HOST-02
  - INST-01
  - INST-02
user_setup:
  - service: github
    why: "Push to GitHub and enable GitHub Pages"
    env_vars: []
    dashboard_config:
      - task: "Enable GitHub Pages from main branch, / (root)"
        location: "GitHub repo Settings > Pages > Build and deployment"
must_haves:
  truths:
    - "PWA is accessible at https://unseenpleasures.github.io/kiosk-app/"
    - "Service worker registers and caches app shell on GitHub Pages"
    - "PWA installs via Add to Home Screen on iPad #1"
    - "PWA installs via Add to Home Screen on iPad #2"
    - "App launches in standalone mode and completes Shopify sync on each iPad"
  artifacts:
    - path: ".planning/INSTALL-GUIDE.md"
      provides: "Step-by-step iPad installation and Guided Access instructions"
      contains: "Add to Home Screen"
  key_links:
    - from: "GitHub Pages"
      to: "https://unseenpleasures.github.io/kiosk-app/"
      via: "main branch deployment"
      pattern: "unseenpleasures.github.io/kiosk-app"
    - from: "iPad Safari"
      to: "GitHub Pages URL"
      via: "Add to Home Screen"
      pattern: "standalone"
---

<objective>
Deploy the PWA to GitHub Pages and create an iPad installation guide. After deployment, verify hosting works. Physical iPad installation is a manual step guided by the install guide.

Purpose: This completes the v1.1 milestone — the app goes live on a public URL and gets installed on the event iPads.

Output: Live PWA at https://unseenpleasures.github.io/kiosk-app/, install guide at .planning/INSTALL-GUIDE.md
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-deploy-and-install/08-CONTEXT.md
@.planning/phases/08-deploy-and-install/08-RESEARCH.md
@.planning/phases/08-deploy-and-install/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy PWA to GitHub Pages</name>
  <files></files>
  <read_first>.gitignore, manifest.json, sw.js</read_first>
  <action>
Deploy the PWA to GitHub Pages (per D-01, D-02, D-03, D-04). Execute these steps in order:

**Step 1: Rename branch (per research open question #3)**
The local branch is `master` but D-03 specifies deploying from `main`:
```bash
git branch -m master main
```

**Step 2: Create GitHub repository (per D-01, D-02)**
Create a public repository named `kiosk-app` under the `unseenpleasures` GitHub account:
```bash
gh repo create unseenpleasures/kiosk-app --public --source=. --remote=origin
```
If `gh` CLI is not available or not authenticated, fall back to:
```bash
git remote add origin https://github.com/unseenpleasures/kiosk-app.git
```
The user will need to create the repo manually on GitHub first in that case.

**Step 3: Stage and commit all PWA files**
Stage all tracked PWA files (the .gitignore from Plan 01 will exclude .planning/, generate-assets.js, project/):
```bash
git add -A
git status
```
Review that only PWA files are staged (no .planning/ files, no generate-assets.js, no project/ directory). Then commit:
```bash
git commit -m "chore: prepare PWA for GitHub Pages deployment

- All paths converted to relative (./) for subdirectory hosting
- Cache version bumped to kiosk-v9
- .gitignore excludes non-PWA files"
```

**Step 4: Push to GitHub**
```bash
git push -u origin main
```
If push fails due to authentication, create a checkpoint for the user to authenticate and retry.

**Step 5: Enable GitHub Pages**
If `gh` CLI is available:
```bash
gh api repos/unseenpleasures/kiosk-app/pages -X POST -f source.branch=main -f source.path=/
```
If the API call fails (Pages may need to be enabled via the GitHub UI), note it for the checkpoint.

**Step 6: Verify deployment**
Wait 60-120 seconds for GitHub Pages to build, then verify:
```bash
curl -s -o /dev/null -w "%{http_code}" https://unseenpleasures.github.io/kiosk-app/
```
Expected: HTTP 200. If 404, wait another 60 seconds and retry.

**Important notes:**
- The repo must be PUBLIC (per D-02) for GitHub Pages free tier
- Deploy from `main` branch, root `/` directory (per D-03)
- Final URL: `https://unseenpleasures.github.io/kiosk-app/` (per D-04)
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://unseenpleasures.github.io/kiosk-app/ 2>/dev/null || echo "Cannot reach URL — verify manually"</automated>
  </verify>
  <acceptance_criteria>
- Git branch is named `main` (not `master`)
- Remote `origin` points to `https://github.com/unseenpleasures/kiosk-app.git`
- All PWA files are committed and pushed to `main`
- No .planning/ files, generate-assets.js, or project/ directory in the pushed commit
- GitHub Pages is enabled from `main` branch, root path
- `https://unseenpleasures.github.io/kiosk-app/` returns HTTP 200
  </acceptance_criteria>
  <done>PWA is live at https://unseenpleasures.github.io/kiosk-app/ and loads correctly in a browser.</done>
</task>

<task type="auto">
  <name>Task 2: Create iPad installation and Guided Access guide</name>
  <files>.planning/INSTALL-GUIDE.md</files>
  <read_first>.planning/phases/08-deploy-and-install/08-RESEARCH.md</read_first>
  <action>
Create `.planning/INSTALL-GUIDE.md` (per D-12, D-13) with the following content. This is a reference document for the user, NOT deployed with the app:

The guide must have these sections with this exact content:

**Title:** `# iPad Installation and Kiosk Setup Guide`

**Section 1: Prerequisites**
- Both iPads are iPad Pro 12.9" 1st Gen (A1652, A9X chip) running iPadOS 16
- WiFi connection available for initial setup (the app works offline after first sync)
- PWA is live at `https://unseenpleasures.github.io/kiosk-app/`

**Section 2: Install PWA on iPad (repeat for each iPad)**
Step-by-step numbered instructions:
1. Open **Safari** on the iPad (MUST be Safari — Chrome/Firefox will not install as a standalone PWA on iPadOS)
2. Navigate to `https://unseenpleasures.github.io/kiosk-app/`
3. Wait for the app to fully load — the service worker needs a few seconds to cache all assets on first visit
4. Tap the **Share button** (box with upward arrow, in the Safari toolbar)
5. Scroll down in the share sheet and tap **"Add to Home Screen"**
6. Optionally edit the name (default: "ID Cards"), then tap **"Add"**
7. The app icon appears on the home screen

**Section 3: Verify Installation**
1. Tap the app icon on the home screen
2. The app should launch in **standalone mode** — no Safari address bar or navigation controls visible
3. The app should show the catalogue screen (or sync prompt if no products cached)
4. If this is the first launch, ensure WiFi is on — the app needs to perform an initial Shopify product sync
5. After sync completes, verify products appear in the catalogue grid
6. Put the iPad into airplane mode and verify the app still loads and browses correctly (offline test)

**Section 4: Configure Guided Access (Kiosk Lockdown)**
One-time setup per iPad:
1. Go to **Settings > Accessibility > Guided Access**
2. Toggle **Guided Access** to ON
3. Tap **Passcode Settings** > **Set Guided Access Passcode** — set a memorable passcode
4. Optional: Under **Time Limits**, set a sound or spoken warning if desired

Per-event activation:
1. Launch the kiosk PWA from the home screen
2. **Triple-press the Home button** (iPad Pro 12.9" 1st Gen has a physical Home button)
3. Guided Access options appear — optionally circle screen areas to disable touch
4. Tap **Start** (top-right corner)
5. The app is now locked — customers cannot exit or switch apps

Exit Guided Access:
1. **Triple-press the Home button**
2. Enter the Guided Access passcode
3. Tap **End** (top-left corner)

**Section 5: Pre-Event Checklist**
- [ ] WiFi connected
- [ ] Open app and trigger Shopify sync from admin panel (Admin > Sync Products)
- [ ] Verify new products appear in the catalogue
- [ ] Set the event name in admin (Admin > Settings > Event Name)
- [ ] Clear email list if needed (Admin > Emails > Export first, then clear)
- [ ] Enable Guided Access on each iPad
- [ ] Verify both iPads are charged or plugged in

**Section 6: Post-Event Checklist**
- [ ] Exit Guided Access on each iPad
- [ ] Export email signups as CSV (Admin > Emails > Export CSV)
- [ ] Note analytics (Admin > Analytics) for event review
- [ ] Optional: clear analytics data for next event
  </action>
  <verify>
    <automated>test -f "C:/kiosk app/.planning/INSTALL-GUIDE.md" && grep -c "Add to Home Screen" "C:/kiosk app/.planning/INSTALL-GUIDE.md" && grep "Guided Access" "C:/kiosk app/.planning/INSTALL-GUIDE.md" > /dev/null && grep "Triple-press" "C:/kiosk app/.planning/INSTALL-GUIDE.md" > /dev/null && echo "PASS: Install guide exists with key content" || echo "FAIL: Install guide missing or incomplete"</automated>
  </verify>
  <acceptance_criteria>
- .planning/INSTALL-GUIDE.md exists
- Guide contains "Add to Home Screen" instructions
- Guide contains "Guided Access" setup instructions
- Guide specifies Safari as the required browser
- Guide mentions `https://unseenpleasures.github.io/kiosk-app/` as the URL
- Guide includes Pre-Event and Post-Event checklists
- Guide mentions "Triple-press the Home button" for Guided Access
- Guide mentions standalone mode verification
  </acceptance_criteria>
  <done>Comprehensive iPad installation and Guided Access guide created at .planning/INSTALL-GUIDE.md with all steps from research patterns 4 and 5.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify GitHub Pages deployment and iPad installation</name>
  <files></files>
  <action>
Human verification checkpoint. No automated action — user performs physical verification on iPads and confirms deployment works.
  </action>
  <what-built>
PWA deployed to GitHub Pages. Install guide created. The app should be live and ready for iPad installation.
  </what-built>
  <how-to-verify>
**Verify hosting (HOST-01, HOST-02):**
1. Open `https://unseenpleasures.github.io/kiosk-app/` in any desktop browser
2. Confirm the app loads (dark background, catalogue or sync prompt appears)
3. Open browser dev tools > Application tab > Service Workers — confirm SW is registered and active
4. Open dev tools > Application > Manifest — confirm manifest loads with `start_url: "./"` and `display: standalone`

**Verify iPad installation (INST-01, INST-02):**
Follow the steps in `.planning/INSTALL-GUIDE.md` on each iPad:
1. Open Safari on iPad #1, navigate to the URL
2. Add to Home Screen
3. Launch from home screen — verify standalone mode (no Safari chrome)
4. Perform initial Shopify sync
5. Verify offline functionality (airplane mode test)
6. Repeat steps 1-5 on iPad #2
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification — user confirms deployment and iPad installation"</automated>
  </verify>
  <done>User has confirmed: (1) PWA loads at GitHub Pages URL, (2) SW registers, (3) both iPads installed and working in standalone mode.</done>
  <resume-signal>Type "approved" if both iPads are set up, or describe any issues encountered.</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. https://unseenpleasures.github.io/kiosk-app/ returns 200 and loads the PWA
2. Service worker registers and caches app shell from GitHub Pages
3. Both iPads have the app installed via Add to Home Screen
4. App launches in standalone mode on both iPads
5. Initial Shopify sync completes successfully on each iPad
6. App works offline after sync (airplane mode test)
</verification>

<success_criteria>
- PWA live at https://unseenpleasures.github.io/kiosk-app/ (HOST-01)
- SW and manifest serve correctly from GitHub Pages (HOST-02)
- iPad #1 installed and functional (INST-01)
- iPad #2 installed and functional (INST-02)
- Install guide created at .planning/INSTALL-GUIDE.md (D-12, D-13)
</success_criteria>

<output>
After completion, create `.planning/phases/08-deploy-and-install/08-02-SUMMARY.md`
</output>
